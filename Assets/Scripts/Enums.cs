public enum ResourceType
{
    Population,
    Energy,
    Food,
    Money,
    Metal,
    MetalPremium,
    Gem
}

public enum BuildingType
{
    HQ,
    LivingSpace,
    MinerBuilding,
    ScoutBuilding,
    RocketSite
}

public enum SoundType
{
    ButtonClick,
}